# Copy to data/config/detection.yaml (create folder if needed).
#
# AquaForge is the only detector: full-scene tiled inference (overlap + NMS). Unknown top-level
# keys from older configs (e.g. backend, yolo, keypoints, wake_fusion) are ignored at load time.

# aquaforge:
#   weights_path: data/models/aquaforge/aquaforge.pt
#   onnx_path: data/models/aquaforge/aquaforge.onnx
#   use_onnx_inference: false
#   onnx_quantize: false
#   imgsz: 512
#   chip_half: 320
#   conf_threshold: 0.15
#   chip_batch_size: 6
#   min_direct_heading_confidence: 0.35
#   tiled_overlap_fraction: 0.5
#   tiled_nms_iou: 0.45
#   tiled_min_proposal_confidence: 0.08
#   tiled_max_detections: 500

# onnx_runtime:
#   intra_op_num_threads: 0
#   inter_op_num_threads: 0
#   execution_mode: parallel
#   graph_optimization_level: all

# Skip full mask/keypoint decode when AquaForge P(vessel) at the spot is below this (0–1).
# min_vessel_proba_for_full_decode: 0.2
# (Alias still accepted: sota_min_hybrid_proba_for_expensive.)
# onnx_providers: ["CUDAExecutionProvider", "CPUExecutionProvider"]
# ui_require_checkbox_for_sota: false
# ui_lazy_sota_overlays: false
