[pytest]
# Third-party autoloaded plugins can break Torch/GDAL-heavy tests (e.g. full-scene JP2 inference).
# - seleniumbase: broad pytest hooks
# - anyio: pytest_anyio can alter async/fixture behavior seen with unittest order + heavy imports
addopts = -p no:seleniumbase -p no:anyio
