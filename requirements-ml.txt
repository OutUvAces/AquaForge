# Optional: PyTorch, pretrained-graph loader, ONNX runtime (AquaForge training / GPU inference). Review UI can use CPU-only requirements.txt alone.
# Install (from project root):  py -3 -m pip install -r requirements-ml.txt
#
# Supported: Python 3.10–3.12 (recommended). 3.13+ may work when PyTorch publishes wheels.
# If pip cannot find "torch" for your Python, install Python 3.12 (64-bit), create a venv,
# then pip install -r requirements.txt and -r requirements-ml.txt, and run Streamlit with that venv.

torch>=2.1.0
torchvision>=0.16.0
# For `aquaforge_backbone` training, also install: py -3 -m pip install -r requirements-backbone.txt
huggingface_hub>=0.20.0
onnxruntime>=1.16.0
